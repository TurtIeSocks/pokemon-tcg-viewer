# Optional pokemontcg.io API key. Without one, the API is rate-limited to
# ~1000 requests/day per IP. Grab a free key at https://dev.pokemontcg.io
# and bump the cap to ~20k/day.
#
# Copy this file to `.env.local` (gitignored) and fill in the value.
VITE_POKEMONTCG_API_KEY=
