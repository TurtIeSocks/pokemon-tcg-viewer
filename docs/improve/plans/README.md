# Implementation Plans

Generated by the improve skill on 2026-07-02 (TanStack best-practices + perf/race + card-renderer-DRY audit, commit `5fe6f08`). Plans 006–007 added 2026-07-06 (paid-service production-readiness audit across the main repo @ `5cd3d09` and `card-stack-cloud` @ `80b2437`; user pre-selected scope = everything needed to launch, split by actor). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Non-interactive run: plans were selected as the top findings by leverage (user pre-authorized autonomous execution); 005 written but deferred by advisor judgment.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Deterministic full `bun test` (order-dependent pollution) + format drift | P1 | M | — | DONE (2 leaks fixed: corpus-loader fetch mock; i18n-active-hooks unseeded loadCorpus → real network) — REGRESSED 2026-07-06: history-runtime.test.ts leaks again; re-fix is 006 Task 11 |
| 002  | Binder rule labels via Maps (kill 20k-card scans) | P2 | S | — | DONE (`e351cc7`) |
| 003  | Shared `cardThumbSrc` helper + command-palette blank-image fix | P2 | S | — | DONE (1 revision round; scope extended to card-grid-island/flip-card) |
| 004  | Async race guards (syncDetail re-enable; profile lost-update) | P3 | S | 001 | DONE (scoped-verified; full-suite gate rides on 001) |
| 005  | Split stack-edit-form.tsx (~500-line convention) | P3 | M | 001 | DEFERRED (execute only on explicit request) |
| 006  | Paid-service launch readiness — code side (deploy plugin wiring, health gate, billing/sync hardening, GDPR deletion, docs, smoke kit) | P1 | L | — | TODO |
| 007  | Human owner's launch checklist (accounts, tokens, dashboards, legal) | P1 | L | interlocks with 006 (see chain diagram in the plan) | TODO (human-only — not executor-runnable) |

## Dependency notes

- 004 depends on 001 because its done criteria require a deterministic full-suite green.
- 001 floats first as the verification baseline for everything else.
- 006 Task 5 (hardening migration) must merge before 007 §B.3 (`supabase db push` to production).
- 007 §D (secrets) must complete before 006 Task 1's deploy path actually ships billing (`DEPLOY_BILLING=1` gate keeps deploys green meanwhile).
- 007 §F.1 (smoke test) consumes 006 Task 10's kit; the billing_enabled flip (§F.2) is the launch moment.

## Findings considered and rejected

### From the 2026-07-06 paid-service audit

- **"Promise.all partial push failure advances watermark / loses data" (sync auditor)**: false — `clearDirty` and `setWatermark` both run *after* the push `Promise.all`; a rejection skips both, rows stay dirty, retry is safe. The real (different) issue — pushed timestamps advancing the watermark past concurrent writes — is 006 Task 4.
- **"Webhook signature verification is substring search, not constant-time" (critic)**: false — verification is `stripe.webhooks.constructEventAsync` (the SDK's HMAC verify), card-stack-cloud `src/webhook.ts:24`.
- **Cohort/percentage feature-flag system for staged billing rollout (critic)**: over-engineering for a solo-operated launch. `billing_config.billing_enabled` + the test-mode smoke kit + a self-purchase (007 §F) is the right-sized rollout; the flag doubles as the kill switch.
- **String-based ISO comparison in `maxIso` (sync auditor)**: safe in practice — all compared values are Postgres-rendered timestamps of uniform format from the same source; the epoch-0 seed compares correctly on the year. Recorded as a maintenance note only.
- **Portal stale-customer 404 (plugin auditor)**: acceptable v1 — the portal button only renders for users with a customer row; a manually-deleted-in-Stripe customer is an operator error handled via the runbook.
- **Pulled-row schema validation before reconcile (sync auditor)**: rows are the user's own (RLS-scoped), written by this same app; malformed rows require the user attacking themselves. Not worth code.
- **"Multiple active subscriptions per user make entitlement ambiguous" (critic)**: `is_pro` is EXISTS-based (any active row) and checkout reuses the mapped customer, so duplicates require manual dashboard action; reconcile (006 Task 3b) drains all subs anyway. Revisit only if multi-tier lands.
- **Env validation of Stripe price-id liveness at startup (plugin auditor)**: the smoke test (006 Task 10 / 007 §F.1) catches misconfigured prices before launch; a startup Stripe API call adds a boot-time network dependency for marginal gain.

- **loaderDeps "missing params" on `$card.tsx` / `$set/index.tsx`** (subagent CORRECTNESS-04/05): false positive — TanStack Router keys route matches on path params and always re-runs loaders when they change; `loaderDeps` receives only `{ search }` by API design. No bug.
- **History-state `cardTab` runtime validation / card-slug regex** (TANSTACK-01/02): no reachable impact path — unknown slugs resolve to `undefined` → handled `notFound`; TS-level enum + error boundary cover the rest.
- **`head()` optional-chaining inconsistency** (TANSTACK-03): loader throws `notFound()` first; defensive chaining is fine as-is.
- **Imperative `navigate()` for search-param updates** (TANSTACK-06): idiomatic; no action.
- **Dirty-set read-modify-write in cache-repo** (RACE-03): mitigated by conditional `clearDirty` (only snapshotted-AND-pushed ids are cleared); worst case is one debounce-cycle latency. Not worth code.
- **srcSet string rebuilds in holo-card** (PERF-3/9): string concat is negligible; React Compiler memoizes the render. Not worth code.
- **Fuzzy-search O(n) per keystroke** (PERF-6/7): already behind `useDeferredValue` + a query cache + length-pruned edit distance; no measurement showing jank. Re-open only with a profile in hand.
- **`getBoundingClientRect` per pointermove in use-holo-effect** (PERF-8): holo vars affect paint/composite, not layout, so the read doesn't force reflow; spring already rAF-driven; single hovered element only. Not worth code.
- **Port HoloCard image FSM into the SSR island fallback** (DEBT-03): fallback is documented-intentional ("crawlable, no hydration risk"); full FSM port is high-risk for a rare 404 flash. The shared-src part is covered by plan 003.
- **Replace owned-missing-grid tiles with full HoloCard** (DEBT-04): grid is deliberately lightweight/non-virtualized for binder-sized lists; foil FX per mini-tile would cost more than the duplication. Shared-src covered by plan 003.
- **Move server types to src/lib re-exports** (DEBT-05): `import type` from server modules is erased at compile time; no runtime coupling exists. Churn without payoff.
- **Split `src/components/ui/sidebar.tsx` (729 lines)**: vendored shadcn primitive; splitting diverges from upstream and complicates regeneration.
- **`syncDetail`-style guard in `enableOffline`**: user-initiated action; no race against user intent.
- **loadCorpus fetch dedupe**: verified correct (per-region in-flight promise map) — recorded here so nobody re-audits it.
